{
  "name": "elyxie",
  "version": "1.0.0",
  "private": true,
  "description": "Elyxie marketing site — static one-pager (React + Three.js).",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch"
  },
  "devDependencies": {
    "esbuild": "^0.20.2"
  }
}
